# Configs

This directory contains checked-in presets for benchmark and VNN-COMP runs.
Preset files should be deterministic, reviewable, and tied to a named workload.
Do not commit local machine paths, report output, credentials, or generated
run logs.
