# Proofs

Proof harnesses are kept outside the default Cargo workspace so normal
development checks stay fast and self-contained. Run proof-specific tools from
their subdirectories and keep generated proof output out of git.
