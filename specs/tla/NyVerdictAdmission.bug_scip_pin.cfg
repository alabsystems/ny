\* CHECK variant — reintroduces the HISTORICAL sat_relu SCIP config-only pin
\* (docs/BEYOND_SOTA_PLAN.md "Build-drift landmine"): the preset pins a solver
\* whose feature is compiled out and the degrade leaves NO visible record.
\* EXPECTED RESULT: NoSilentLaneLoss is VIOLATED with a counterexample trace.
CONSTANT BugConfigOnlyPin = TRUE
CONSTANT BugPhantomMipLane = FALSE
CONSTANT BugUncheckedRup = FALSE
CONSTANT BugSkipOrtGate = FALSE
CONSTANT BugUnsoundGpuVerdict = FALSE

INIT Init
NEXT Next

INVARIANT NoSilentLaneLoss
