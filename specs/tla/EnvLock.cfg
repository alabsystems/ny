\* FIXED variant: the dispatch_conv2d reader holds the shared env lock with the
\* var scoped to unset (with_serialized_env_vars_removed). Every invariant must
\* hold exhaustively.
CONSTANT Writers = {"w1", "w2"}
CONSTANT Reader = "r"
CONSTANT ReaderHoldsLock = TRUE

INIT Init
NEXT Next

INVARIANT TypeOK
INVARIANT LockOwnership
INVARIANT MutualExclusion
INVARIANT ReaderSeesDefault
