\* OLD BUGGY variant: the dispatch_conv2d reader consults the process-global
\* env cell mid-assertion WITHOUT the shared env lock (tests_engine.rs comment).
\* ReaderSeesDefault MUST fail with a writer-set / reader-read interleaving
\* counterexample; the lock invariants themselves still hold (the bug is
\* reading outside the critical section, not breaking the mutex).
CONSTANT Writers = {"w1", "w2"}
CONSTANT Reader = "r"
CONSTANT ReaderHoldsLock = FALSE

INIT Init
NEXT Next

INVARIANT TypeOK
INVARIANT LockOwnership
INVARIANT MutualExclusion
INVARIANT ReaderSeesDefault
