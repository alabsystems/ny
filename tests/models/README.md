# Test Models

Small ONNX, NNet, and VNN-LIB fixtures used by integration tests. Keep fixtures
minimal and deterministic. Large downloaded benchmark corpora should live
outside the repository and be referenced by environment variables or manifests.
